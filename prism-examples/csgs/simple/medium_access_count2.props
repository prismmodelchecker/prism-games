const int k;

// each player wants to maximise the probability of sending smax messages within k steps
<<p1:p2>>max=? (P[F<=k sent1=smax] + P[F<=k sent2=smax])

// each players want to maximise the probability of sending smax messages
<<p1:p2>>max=? (P[F sent1=smax] + P[F sent2=smax])

// one bounded and one unbounded
<<p1:p2>>max=? (P[F<=k sent1=smax] + P[F sent2=smax])
